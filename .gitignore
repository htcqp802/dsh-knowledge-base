# ---- 依赖 ----
node_modules/
*.tsbuildinfo
# 注意：lib/（构建产物）随仓库提交——CI 的 Trusted Publishing 直接发布 lib，
# 避免在 CI 里重装 @deepseek-ai 依赖（npm 仅有 rc 版，与本地 checkout 不一致）。

# ---- 本地运行数据（绝不提交）----
.dsh-home/
.test-workspace/
.test-home/

# ---- 系统/编辑器 ----
.DS_Store
*.log
.env
.env.local

# ---- 打包产物 ----
*.tgz
dist/
